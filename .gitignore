**/.idea/
.DS_Store
_build/
.claude/

# Generated by build_slides.sh -- rebuilt fresh by every jb build, not
# meant to be committed (this is where the ~27MB-per-deck WASM bundles
# would otherwise bloat the repo).
_slides_build/

# marimo's own local session/state files, not part of the deck source.
**/__marimo__/
